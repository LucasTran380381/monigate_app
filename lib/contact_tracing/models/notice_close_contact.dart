class NoticeCloseContact {
  DateTime dateReceived;
  DateTime dateLimit;
  int dateRange;
  List<DateTime> noticedDate;

  NoticeCloseContact(this.dateReceived, this.dateLimit, this.dateRange, this.noticedDate);
}
